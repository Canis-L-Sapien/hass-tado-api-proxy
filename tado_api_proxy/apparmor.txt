#include <tunables/global>

profile tado_api_proxy flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  # Capabilities
  file,
  signal (send) set=(kill,term,int,hup,cont),

  # Network
  network inet tcp,
  network inet udp,
  network inet6 tcp,
  network inet6 udp,

  # Data directory
  /data/                r,
  /data/**              rw,

  # Binary
  /app                  mr,

  # Required by PocketBase / Go runtime
  /proc/sys/net/**      r,
  /proc/*/net/**        r,
  /dev/urandom          r,
  /tmp/**               rw,
}
